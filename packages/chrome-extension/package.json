{
  "name": "codefunc-chrome",
  "version": "1.0.0",
  "description": "CodeFunc Chrome extension for GitHub file overviews",
  "license": "AGPL-3.0-only",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "package": "node build.js && cd dist && zip -FS -r ../codefunc-chrome-v0.1.0.zip . -x '*.DS_Store'"
  },
  "dependencies": {
    "@codefunc/core": "*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.28.2",
    "typescript": "^5.4.5"
  }
}
