{
  "name": "@codefunc/core",
  "version": "1.0.0",
  "description": "Platform-agnostic engine for CodeFunc: parsing, caching, and Gemini AI summarization",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/tests/**/*.test.js"
  },
  "dependencies": {
    "@google/genai": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "typescript": "^5.4.5"
  }
}
